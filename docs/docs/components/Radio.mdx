---
title: 'Radio'
---

import { RadioGroup, Radio } from 'minerva-ui';

```js
import { RadioGroup, Radio } from 'minerva-ui';
```

```jsx live
() => {
  const [value, setValue] = React.useState('supreme');
  return (
    <>
      <Heading as="h3" fontSize="md" fontWeight={600} mb={3}>
        Pizza Toppings
      </Heading>
      <RadioGroup onChange={e => setValue(e.target.value)} value={value}>
        <Radio value="supreme">Supreme</Radio>
        <Radio value="meat">Meat</Radio>
        <Radio value="vegetarian">Vegetarian</Radio>
      </RadioGroup>
    </>
  );
};
```
