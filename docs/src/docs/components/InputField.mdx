---
title: 'InputField'
---

import { Input, InputField } from 'minerva-ui';

`<InputField />` is an wrapper component for an `<Input />` that includes label and error messages.

It can be compared to the `form-control` class from Bootstrap.

```js
import { InputField, Input } from 'minerva-ui';
```

```jsx live
() => {
  const [name, setName] = React.useState('');
  const [error, setError] = React.useState('');

  return (
    <InputField label="Label" errorMsg={error}>
      <Input
        placeholder="Basic Input"
        value={name}
        onBlur={!name ? () => setError('This field cannot be empty') : null}
        onChange={e => {
          setError('');
          setName(e.target.value);
        }}
      ></Input>
    </InputField>
  );
};
```

## Input Field with Error Message

```jsx live
() => {
  const [name, setName] = React.useState('');
  const [error, setError] = React.useState('');

  return (
    <InputField
      label="Label"
      htmlFor="basicInput"
      errorMsg={error}
      errorStyles={{ color: '#FF8247', size: '16px' }}
    >
      <Input
        id="basicInput"
        placeholder="Basic Input"
        value={name}
        onBlur={!name ? () => setError('This field cannot be empty') : null}
        onChange={e => {
          setError('');
          setName(e.target.value);
        }}
      ></Input>
    </InputField>
  );
};
```

## Required Input Field

```jsx live
() => {
  const [name, setName] = React.useState('');
  const [error, setError] = React.useState('');

  return (
    <InputField
      label="Label"
      htmlFor="basicInput"
      isRequired
      requiredMarkerColor="#FF6347"
      errorMsg={error}
    >
      <Input
        id="basicInput"
        placeholder="Basic Input"
        value={name}
        onBlur={!name ? () => setError('This field cannot be empty') : null}
        onChange={e => {
          setError('');
          setName(e.target.value);
        }}
      ></Input>
    </InputField>
  );
};
```
