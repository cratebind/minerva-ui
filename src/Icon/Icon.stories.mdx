import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Icon from '../Icon';

<Meta title="Elements|Icon" component={Icon} />

# Icon

Basic image component, replaces `<img>` tag.

```js
import { Icon } from 'minerva-ui'
```

```jsx
<Icon src="https://source.unsplash.com/random" maxWidth="20rem" />
```

<Props of={Icon} />