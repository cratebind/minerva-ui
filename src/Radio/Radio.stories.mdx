import { useState } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { RadioGroup, Radio } from '../Radio';

<Meta title="Elements|Radio" component={Radio} />

# Radio

```js
import { RadioGroup, Radio } from 'minerva-ui';
```

```jsx
() => {
  const [value, setValue] = React.useState('test');
  return (
    <RadioGroup onChange={e => setValue(e.target.value)} selectedValue={value}>
      <Radio value={'test'}>test</Radio>
      <Radio value={'test1'}>test1</Radio>
      <Radio value={'test2'}>test2</Radio>
    </RadioGroup>
  );
};
```

#### RadioGroup Props
<Props of={RadioGroup} />

#### Radio Props
<Props of={Radio} />
