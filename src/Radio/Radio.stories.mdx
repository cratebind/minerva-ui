import { useState } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { RadioGroup, Radio } from '../Radio';

<Meta title="Elements|Radio" component={Radio} />

# Radio

```js
import { RadioGroup, Radio } from 'minerva-ui';
```

```jsx
() => {
  const [value, setValue] = React.useState('test');
  return (
    <>
    <Heading size="sm" fontWeight={600} mb={3}>Pizza Toppings</Heading>
    <RadioGroup onChange={e => setValue(e.target.value)} value={value}>
      <Radio value="test">
        Meat
      </Radio>
      <Radio value="test1">
        Vegetarian
      </Radio>
      <Radio value="test2">
        Supreme
      </Radio>
    </RadioGroup>
    </>
  );
};
```

#### RadioGroup Props
<Props of={RadioGroup} />

#### Radio Props
<Props of={Radio} />
