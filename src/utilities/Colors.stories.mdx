import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import baseTheme from '../theme';
import ColorTag from '../../utils/ColorTag';

<Meta title="Utilities|Colors" />

# Colors

```jsx
() => (
  <Flex
    backgroundColor="#edf2f7"
    padding="10px"
    justifyContent="space-between"
  >
    <Button bg="teal.500">teal.500</Button>
    <Button bg="purple.800">purple.800</Button>
    <Button bg="red.900">red.900</Button>
    <Button bg="indigo.400">indigo.400</Button>
    <Button bg="gray.600">gray.600</Button>
    <Button
      bg="transparent"
      borderWidth="1px"
      color="gray.600"
    >transparent</Button>
  </Flex>
)
```

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>
    {Object.entries(baseTheme.colors).map(([color, value]) => (
      <tr>
        <td><pre>{color}</pre></td>
        <td>
          {typeof value === 'string' ? <ColorTag color={['transparent', '#fff'].includes(value) ? '#000' : '#fff'} code={value}>{value}</ColorTag> : Object.entries(value).map(([number, code]) => (
            <ColorTag number={number} code={code}>{number}: {code}</ColorTag>
          ))}
        </td>
      </tr>
    ))}
  </tbody>
</table>