import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import baseTheme from '../theme';

<Meta title="Utilities|Border Radius" />

# Border Radius

## Aliases

```jsx
() => (
  <Flex
    backgroundColor="#edf2f7"
    padding="10px"
    justifyContent="space-between"
  >
    <Button borderRadius="none">none</Button>
    <Button borderRadius="sm">sm</Button>
    <Button borderRadius="default">default</Button>
    <Button borderRadius="md">md</Button>
    <Button borderRadius="lg">lg</Button>
    <Button borderRadius="full">full</Button>
  </Flex>
)
```

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>
    {Object.entries(baseTheme.radii).map(([name, value]) => (
      <tr>
        <td>{name}</td>
        <td>
          <span>{value}</span>
        </td>
      </tr>
    ))}
  </tbody>
</table>

## Custom Props
```jsx isManual=true
const Card = (props) => <Flex p={5} bg="white" borderWidth="1px" alignItems="center" justifyContent="center" {...props} />

render(
  <Flex
    backgroundColor="#edf2f7"
    padding="10px"
    justifyContent="space-between"
  >
    <Card radiusLeft="4px">Radius Left: 4px</Card>
    <Card radiusRight="md">Radius Right: 'md'</Card>
    <Card radiusTop="md">Radius Top: 'md'</Card>
    <Card radiusBottom="md">Radius Bottom: 'md'</Card>
  </Flex>
)
```